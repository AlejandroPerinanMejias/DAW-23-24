let registro = new Map();

function throw_throw() {
    let n = parseInt(Math.random() * (7));
    let n2 = parseInt(Math.random() * (7));
    return n + n2;
}

for (let i = 0; i < 35999; i++) {
    let result = throw_throw();
    if (registro.keys) {}
    
}